---
const { serviceName, serviceUrl, serviceIcon } = Astro.props;
---

<a href={serviceUrl} class="no-underline w-fit h-fit font-sans m-2 grow">
    <div class="flex flex-col justify-center h-full items-left p-4 bg-zinc-50/10 border-2 border-zinc-50/10 rounded-2xl w-full h-full">
        <h1 class="text-zinc-50">{serviceName}</h1>
    </div>
</a>